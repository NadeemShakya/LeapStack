import React from 'react';

import SideBar from '../sidebar';
import HomeRouter from './HomeRouter';

/**
 * Component to view all the users.
 *
 */
const Home = () => (
  <>
    <SideBar />
    <HomeRouter />
  </>
);

export default Home;
